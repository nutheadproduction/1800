import { DropModDirective } from './drop-mod.directive';

describe('DropModDirective', () => {
  it('should create an instance', () => {
    const directive = new DropModDirective();
    expect(directive).toBeTruthy();
  });
});
